# GSC 提交引导：2026-09-08 webp 页「示例结果」层重抓

上线：wrangler Version `72fd4016-ab9f-47b6-844b-129e9a8ddb2e`。

## 需人工请求索引的 URL（1 条）

1. https://image2base64.com/webp-to-base64

## 背景

- GSC URL 检查（2026-09-08 API 实查）：**已收录**（Submitted and indexed，crawl 2026-09-06），非新页，无需首次请求。
- 本次改动 = 新增「Example: a real WebP, converted」示例结果层（真实 236B WebP → 316 字符 Base64 实测案例 + 渲染对照），补哥飞「格式说明 + 工具 + 示例结果」三层结构的第三层。
- 目的 = 让 Google 重抓新内容，不是首次收录。

## 操作步骤

1. 打开 Google Search Console → image2base64.com 资源。
2. 顶部「检查任何网址」框粘贴上述 URL → 回车 → 等抓取 → 点「请求编入索引」（已收录页面点这个按钮就是重抓请求）。
3. 顺带确认 sitemap.xml 无报错（34 locs，webp 页已在列）。

## 页面说明（供复核）

- 目标词：webp to base64 / webp to base64 converter（GSC 前窗 8/25–8/31 已见曝光，排名 62-70——说明 Google 已把该词与该页关联）。
- 页面现三层齐备：工具（拖拽转换）+ 格式说明（explained + 4 个 h3）+ 示例结果（真实转换案例，渲染图即 data URI 本身）。
- title/description 已是「主词前置 + No Upload 差异化钩子」，符合哥飞 CTR 模板，未动。
- 闸门判据：9/15 GSC 复核 webp to base64 词曝光是否 0→起步、排名是否从 62-70 前移；jpg 页 CTR 是否随曝光恢复上涨（当前 4.8% 偏低，曝光未过百前不改 title，避免反复改页违反苗头期纪律）。
