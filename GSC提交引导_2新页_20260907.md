# GSC 手动提交引导 — 2 个新页（2026-09-07 第二批）

> 背景：今日第二批上线 2 个 GSC 挖掘新页（commit `5d59dcd` + 修正 `f2dcf36`，wrangler Version `5d027cb9`，线上已验证 200 + canonical + Playwright 功能实测通过）。sitemap 已含 2 个新 URL 且在线可抓（32 locs 已核）。AI 无法替你做 GSC 提交（需要你的 Google 登录态），以下 1-2 分钟手动流程。

## 第一步：URL 检查逐个加速收录（2 个 URL，约 1-2 分钟）

对下面每个 URL 重复同一操作：

```
https://image2base64.com/tiff-to-base64
https://image2base64.com/how-to-convert-image-to-base64
```

1. GSC 顶部搜索框粘贴 URL → 回车，等检查完成（10-20 秒）
2. 显示「网址不在 Google 上」= 正常（新页）
3. 点 **「请求编入索引（REQUEST INDEXING）」** → 等约 1 分钟 → 「已请求编入索引」即可
4. 换下一个 URL 重复

**注意**：
- URL 检查每日配额约 10-12 次，2 个完全够用
- 弹 reCAPTCHA 就完成验证继续
- sitemap.xml 上次提交仍有效，Google 会按周期重抓（32 个 URL），无需重复提交

## 部署事实（已由 AI 核验，无需重查）

| 项 | 状态 |
|---|---|
| 2 页线上 200 + canonical 自指 + 单 H1 | ✅ curl 已核 |
| sitemap 32 locs 含 2 个新 URL | ✅ 线上已抓到 |
| 全站 23 个工具页 footer + 32 个 Guides-nav 页内链 + 首页 2 张新卡 + 指南区链接 | ✅ 线上抽查通过 |
| /tiff-to-base64 功能实测（真实 TIFF 文件 → data URI，Meta/overhead/HTML/CSS 输出全对） | ✅ Playwright 线上实测 |
| /how-to-convert-image-to-base64 结构实测（9 节 / 8 代码块 / 12 锚点全有效 / 5 FAQ） | ✅ Playwright 线上实测 |
| TIFF FAQ 事实修正（big-endian 签名 TU0AKg，非 TUxAK，对真实文件验证） | ✅ 修正已上线（TUxAK 线上 0 处） |
| check_seo_consistency fail=0 | ✅ |
| JSON-LD 全部有效 | ✅ |

## 提交后预期与监控

| 时间 | 预期 |
|---|---|
| 1-3 天 | GSC「网页」索引报告出现 2 个「已编入索引」 |
| 1-2 周 | `tiff to base64` / `tiff to base64 converter`（28d 5 imp、跨周期持续）与语言教程词簇（js/py/php/java/linux/n8n 合计 16 imp）开始出现这 2 个 URL 的曝光/排名数据 |
| 2 周后 | 若 tiff 簇曝光仍无起色 → 说明新站权重仍是瓶颈，按计划继续外链建设，不改页面 |

## 候选词状态回写

`i2b64_keywords.json`（WorkBuddy 2026-09-03 工作区 `.workbuddy/`）已更新：
- `tiff to base64 converter` → candidates_done（commit 5d59dcd，live /tiff-to-base64）
- `convert image to base64 javascript` → candidates_done（同 commit，live /how-to-convert-image-to-base64，单页覆盖 js/node/python/php/java/linux/n8n 16 imp 簇）
- `image to base64 n8n` → candidates_done（并入教程页 n8n 小节，未建独立页）
- 候选池剩 7 个：image url to base64 converter / css data uri generator / base64 favicon generator / ico to base64 converter / resize image to base64 / base64 to jpg converter / svg favicon generator
