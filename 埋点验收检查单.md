# 埋点验收检查单（v1）

> 站点：image2base64.com（档案目录：本仓库根）  
> 栈：自有 Worker `POST /api/evt` → D1 `image2base64-events`；可选 Cloudflare Web Analytics（边缘注入）  
> **禁止**：引入 GA4 / GTM / 第三方广告像素作为产品埋点  
> 触发：任意新事件名、新参数口径、或改 `worker.js` `EVENT_NAMES` / `app.js` `track(...)` 后上线  
> 教训来源：2026-09-05 `credit` 客户端已发、服务端白名单未收 → 静默丢数至 9/6 才修（见 `SEO_LOG.md`）

---

## 0. 上线前：设计稿对齐（门禁）

- [ ] 设计稿列出：事件名、触发时机、`x1`/`x2` 含义与枚举、所涉页面 path  
- [ ] 设计稿事件名 ⊆ 计划写入的服务端 `EVENT_NAMES`（见下条）——**先扩白名单，再放客户端**  
- [ ] 明确「成功指标」：上线 1h 内 D1 至少应看到哪些 `name`、大致量级（哪怕是人工点几下）  
- [ ] 不引入 GA4/GTM；DebugView 仅指自有 D1 查询或 CF Web Analytics 面板，不是 Google DebugView

---

## 1. 服务端白名单先行（`EVENT_NAMES`）

权威：`worker.js` 内 `EVENT_NAMES`（当前含 `page_view` / `convert` / `copy` / `decode` / `download` / `error` / `sample` / `compress` / `calc` / `credit` / `extract`）。

- [ ] **合并顺序**：PR 先含 Worker 白名单变更（或同 commit 且部署同一版本），再依赖客户端 `track("新名", …)`  
- [ ] 新事件名与设计稿一字不差（大小写、下划线）；禁止「先上前端、回头补白名单」  
- [ ] 部署后核对线上 Worker 版本号 / 部署时间，确认不是「CI 假成功、资产未更新」（历史坑：缺 `CLOUDFLARE_API_TOKEN` 时 Deploy 步骤可静默跳过）  
- [ ] 回归：旧事件仍在白名单内，未误删

**Credit 教训（必读）**：客户端 `track("credit", on|off)` 已上线，但 `EVENT_NAMES` 未含 `credit` → Worker 对未知名直接 204、不入库。验收时绝不能只看 Network 里 beacon 成功。

---

## 2. 上线 1h 内查真实计数（D1）

在真实流量或验收点击后 **1 小时内** 查远程 D1（勿只看本地/`--local`）：

```bash
npx wrangler@4 d1 execute image2base64-events --remote --command \
  "SELECT name, COUNT(*) AS n FROM events WHERE ts >= datetime('now','-1 hour') GROUP BY name ORDER BY n DESC;"
```

（若表用 `created_at` 等字段名，以仓库 schema / deploy 技能样例为准，改列名后再跑。）

- [ ] 设计稿要求的事件名出现，计数 ≥ 验收操作次数（允许少量失败，不许为 0）  
- [ ] 抽 3～5 条明细：`page` / `x1` / `x2` / `country` / `device` 合理  
- [ ] 若同时开了 CF Web Analytics：面板有访问趋势即可，**不以 CF 页览代替 D1 行为事件**

---

## 3. 参数口径对齐设计稿

| 检查项 | 通过标准 |
|--------|----------|
| 事件名 `e` | 与设计稿、与 `EVENT_NAMES` 一致 |
| 页面 `p` | 为 canonical path（注意无 `.html`、307 跳转后 path） |
| `x1` / `x2` | 枚举/桶与设计稿一致（如 sizeBucket、`on`/`off`、mime） |
| 截断 | Worker `clip` 后仍能区分关键档位（勿把有区分度的值截没） |
| 隐私 | 无文件名、无 Base64 正文、无邮箱/IP 入库 |

- [ ] 用验收点击对照设计稿逐字段打勾  
- [ ] `error` 类事件：原因串已截断且不含用户内容

---

## 4. 真实浏览器 DOM 验证 beacon（非 curl）

已知：CF Web Analytics 的 `beacon.min.js` / `/cdn-cgi/rum` **curl 看不到**（边缘只对浏览器注入）；自有 `/api/evt` 应用 `sendBeacon`，也必须以真浏览器为准。

- [ ] 真浏览器（或 CDP/Playwright 连真实页）打开目标页，DevTools → Network  
- [ ] 触发动作后看到 `POST /api/evt`（或 `sendBeacon` 等价请求），status 2xx/204，payload 含预期 `e`/`p`/`x1`/`x2`  
- [ ] **禁止**仅用 `curl -X POST` 代替「页面里 track 是否挂上」；curl 只可作 Worker 通路烟雾测试  
- [ ] 若验 CF Web Analytics：确认 `static.cloudflareinsights.com/beacon.min.js` 与 `/cdn-cgi/rum` 在会话中出现

---

## 5. 未知事件丢弃必须留痕（禁止无痕静默丢）

当前实现：不在 `EVENT_NAMES` 内的 `name` → 不写 `events` 表，仍返回 204（**无痕**）。这是 credit 事故的根因形态。

验收要求（制度；实现未跟上则记「缺口」并排期，不得假装已满足）：

- [ ] 未知/拒绝事件有可查询留痕之一：  
  - D1 旁路表（如 `events_rejected`：name、page、reason、ts），或  
  - Worker 结构化日志（可按部署版本检索），或  
  - 明确的 metrics 计数（rejected_total）  
- [ ] 留痕含：原始事件名（截断后）、拒绝原因（`not_in_whitelist` 等）、时间  
- [ ] 留痕本身仍隐私优先：不落 PII / 文件内容  
- [ ] 验收操作：故意打一个不在白名单的测试名，确认 **能在留痕里看到**，且正式 `events` 表无该名

**现状缺口（v1 记录）**：截至本检查单落档时，`worker.js` 对未知名仍无痕丢弃。新事件上线前必须靠本单 §1+§2 人工兜住；§5 工程化留痕另开任务，完成前每次发版在本单勾「已知缺口、已人工 D1 对账」。

---

## 6. 签字栏（一次上线一行）

| 日期 | 变更摘要 | §1 白名单 | §2 D1 1h | §3 口径 | §4 浏览器 | §5 留痕/已知缺口 | 验收人 |
|------|----------|-----------|----------|---------|-----------|------------------|--------|
| YYYY-MM-DD | | ☐ | ☐ | ☐ | ☐ | ☐ / 缺口已注明 | |

---

## 附录 A · 相关文件

- `worker.js` — `EVENT_NAMES`、`POST /api/evt`、`logEvent`  
- `app.js` — `track` / `sizeBucket` 与各工具挂载点  
- `wrangler.toml` — D1 binding  
- `SEO_LOG.md` — 2026-09-03 埋点上线、2026-09-05 credit、2026-09-06 白名单补齐  
- 查询样例：仓库 deploy 技能 / SEO_LOG「查询方式」节

## 附录 B · 明确不做

- 不接 GA4、GTM、Facebook/Google Ads 像素作产品行为源  
- 不把「Network 204」当成「已入库」  
- 不把 curl 当 CF beacon / 页面 track 的唯一验收  
