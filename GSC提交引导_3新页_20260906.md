# GSC 手动提交引导 — 3 个新页（2026-09-06）

> 背景：今日上线 3 个选词新页（commit `1779f96`，线上已验证 200 + canonical）。sitemap 已含 3 个新 URL 且在线可抓（`https://image2base64.com/sitemap.xml` 已核）。AI 无法替你做 GSC 提交（需要你的 Google 登录态 + 可能的人机验证），以下 2 分钟手动流程。

## 第一步：重新提交 sitemap（一次性，覆盖全部新页）

1. 打开 https://search.google.com/search-console
2. 左上角确认资源为 **image2base64.com**（域资源或 URL 前缀资源均可）
3. 左侧菜单 → **站点地图（Sitemaps）**
4. 在「添加新的站点地图」输入 `sitemap.xml` → 提交
   - 如果之前已提交过 `sitemap.xml`：无需重做，Google 会按周期重新抓取；可点进去看「上次读取日期」和「已发现的网址数」（应为 25）
   - 新提交后状态显示「成功即可」，具体网址数 1-3 天内更新

## 第二步：URL 检查逐个加速收录（3 个 URL，约 2 分钟）

对下面每个 URL 重复同一操作：

```
https://image2base64.com/compress-png-to-base64
https://image2base64.com/base64-image-size-calculator
https://image2base64.com/base64-image-email-signature
```

1. GSC 顶部搜索框（「输入网址以进行检查」）粘贴第一个 URL → 回车
2. 等待检查完成（约 10-20 秒），会显示「网址不在 Google 上」——正常，新页
3. 点 **「请求编入索引（REQUEST INDEXING）」**
4. 弹窗「正在测试是否可以将此编入索引」→ 等约 1 分钟 → 显示「已请求编入索引」即可
5. 换下一个 URL 重复

**注意**：
- URL 检查每日配额约 10-12 次，3 个完全够用
- 如果弹人机验证（reCAPTCHA），完成验证继续即可
- 若某个 URL 检查报「网页无法编入索引」的替代原因，截图发我，我排查页面（可能性低：canonical/robots 均已核过）

## 第三步：验证内链已生效（可选，已由 AI 核过）

- 线上 27 页 footer 均已指向 3 个新页（抽查 png-to-base64 页已确认在线）
- 首页卡片已含 3 个新工具入口

## 执行状态更新（2026-09-06 12:00 CST）

- **索引请求**：Codex 已于 2026-09-06 11:51–11:56 在 GSC 中为 3 个 URL 逐个执行「请求编入索引」，三页均返回 **Indexing requested**。站点资源为 `sc-domain:image2base64.com`。
- **sitemap**：`https://image2base64.com/sitemap.xml` 状态 Success（最后读取 2026-09-01），已发现 16 页；本次未重复提交 sitemap。
- **GSC 数据延迟**：脚本 `~/.workbuddy/scripts/gsc_pull.py` 已验证 `siteFullUser` 权限并拉取 2026-08-30 → 2026-09-05 的真实数据。由于 3 个新页在 9/6 才请求索引，当前 GSC 效果/索引报告**尚未出现**这 3 页（零曝光不等于未收录，收录状态需等 URL Inspection 或索引报告更新）。
- **D1 行为事件**：`compress` / `calc` / `credit` 事件已修复服务端白名单，本日起可正常落库；具体事件数见当月 GSC 机会日报的「行为复查」节。

## 提交后预期与监控

| 时间 | 预期 |
|---|---|
| 1-3 天 | GSC「网页」索引报告出现 3 个「已编入索引」 |
| 1-2 周 | 效果报告开始出现这 3 页的曝光（即使没人点） |
| 2-6 周 | 目标词若有长尾查询 → 点击 0→>0 |

**已有监控**：
- 每日 GSC 机会日报（当前文件 `gsc_data/I2B64_GSC机会日报_2026-09-06.md`）会跟踪 3 页曝光/点击，直到出现有意义变化或需人工处理。
- 每月 5 号 GSC 闸门自动化（D1 事件环比 + 关键词判定）。三个词的成败判据：
  - `/compress-png-to-base64` → `compress png to base64` 点击 >0
  - `/base64-image-size-calculator` → `base64 image size calculator` 点击 >0
  - `/base64-image-email-signature` → `base64 image email signature` 点击 >0
  - 辅助信号：D1 `compress`/`calc` 事件开始出现（worker 白名单已修，本日起有效）

**注意**：不要再次手动请求索引这三页；重复请求不会加速收录，且会消耗每日配额。
