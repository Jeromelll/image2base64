# GSC 手动提交引导 — 3 个新页（2026-09-07）

> 背景：今日上线 3 个选词新页（commit `3c469bd`，wrangler Version `43d44b6b`，线上已验证 200 + canonical + Playwright 功能实测 14/14）。sitemap 已含 3 个新 URL 且在线可抓（28 locs 已核）。AI 无法替你做 GSC 提交（需要你的 Google 登录态），以下 2 分钟手动流程。

## 第一步：URL 检查逐个加速收录（3 个 URL，约 2 分钟）

对下面每个 URL 重复同一操作：

```
https://image2base64.com/image-to-base64-for-api
https://image2base64.com/extract-base64-image-from-css
https://image2base64.com/compress-jpg-to-base64
```

1. GSC 顶部搜索框粘贴 URL → 回车，等检查完成（10-20 秒）
2. 显示「网址不在 Google 上」= 正常（新页）
3. 点 **「请求编入索引（REQUEST INDEXING）」** → 等约 1 分钟 → 「已请求编入索引」即可
4. 换下一个 URL 重复

**注意**：
- URL 检查每日配额约 10-12 次，3 个完全够用
- 弹 reCAPTCHA 就完成验证继续
- sitemap.xml 上次提交仍有效，Google 会按周期重抓（28 个 URL），无需重复提交

## 部署事实（已由 AI 核验，无需重查）

| 项 | 状态 |
|---|---|
| 3 页线上 200 + canonical 自指 | ✅ curl 已核 |
| sitemap 28 locs 含 3 个新 URL | ✅ 线上已抓到 |
| 全站 18 个工具页 footer + 首页 3 张新卡内链 | ✅ 线上抽查通过 |
| app.js 线上为最新版（含 initExtractor/initApiPayloads） | ✅ |
| worker.js 白名单已加 `extract` 事件 | ✅ 随本次部署 |
| 本地 Playwright + 线上 Playwright 功能实测 | ✅ 各 14/14 |

## 提交后预期与监控

| 时间 | 预期 |
|---|---|
| 1-3 天 | GSC「网页」索引报告出现 3 个「已编入索引」 |
| 1-2 周 | 效果报告出现曝光 |
| 2-6 周 | 目标词长尾点击 0→>0 |

**10/5 月度 GSC 闸门的成败判据（追加 3 条）**：
- `/image-to-base64-for-api` → `image to base64 for api` / `base64 image api` 相关查询点击 >0
- `/extract-base64-image-from-css` → `extract base64 image from css` / `extract image from css` 点击 >0
- `/compress-jpg-to-base64` → `compress jpg to base64` 点击 >0
- 辅助信号：D1 `extract` 事件开始落库（白名单本次已放开）

**不要重复请求索引**：重复请求不加速收录，还烧每日配额。
